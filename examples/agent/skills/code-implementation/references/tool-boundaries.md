# Tool Boundaries

External tools may read files, write files, run shell commands, execute Python, search files, list globs, and fetch HTTP content when enabled by configuration.

All implementation claims should be backed by tool output or explicitly marked as unverified.
